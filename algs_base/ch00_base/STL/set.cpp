#include <cstdio>
#include <string>
#include <iostream>
#include <algorithm>
#include <vector>
#include <queue>

using namespace std;

int main()
{
    /*
    size() empty() clear() begin() end() ++ --
    insert() 插入一个数
    find()  不存在返回end迭代器
    count() 查找一个元素的个数
    erase()
     (1)输入2 删除等于2的所有结点 logn
     (2)输入迭代器，删除这个迭代器
    lower_bound() 返回的是大于等于x的最小数
    upper_bound() 返回大于x的最小数
    */
}
